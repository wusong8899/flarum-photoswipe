import app from 'flarum/admin/app';

// Currently no admin interface needed for PhotoSwipe Glide
// This file exists to satisfy the build configuration
app.initializers.add('sycho-photoswipe-admin', () => {
  // No admin functionality required at this time
});